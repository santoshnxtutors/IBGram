import type { CountrySeoPage } from "../types";
import { usa } from "./usa";

/** Registry of every published country landing page, in rollout order. */
export const countrySeoPages: CountrySeoPage[] = [usa];
